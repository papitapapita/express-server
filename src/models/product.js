export class Product {
  constructor(id, name, price, image, isBlocked) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.image = image;
    this.isBlocked = isBlocked;
  }
}
